import { createActions } from 'redux-arc';

const _ = null;

export const { types, creators } = createActions('game', {
  addEntity: _,
  removeEntity: _,
  initBuffer: _,
  start: _,
  move: _,
  surrender: _,
  turn: _,
  end: _,
});